import React from 'react';
import PropTypes from 'prop-types';

const Home = () => {
    return (
        <div>
            <h1 >hello from home</h1>
        </div>
    );
};

Home.propTypes = {
    
};

export default Home;